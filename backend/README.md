# refer-backend

FastAPI + LangGraph backend for the **refer** academic citation review app.
See the repository root [README](../README.md), [research.md](../research.md), and [plan.md](../plan.md).

```powershell
uv sync
uv run uvicorn app.main:app --reload --port 8000
```
